language: ruby
rvm:
  - "2.0.0"
# uncomment this line if your project needs to run something other than `rake`:
# script: bundle exec rspec spec

notifications:
  irc: "irc.tamu.edu#foottraffic"
#  template:
#    - "%{repository} (%{commit}) : %{message} %{foo} "
#    - "Build details: %{build_url}"
